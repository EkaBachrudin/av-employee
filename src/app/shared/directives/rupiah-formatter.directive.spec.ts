import { RupiahFormatterDirective } from './rupiah-formatter.directive';

describe('RupiahFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new RupiahFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
